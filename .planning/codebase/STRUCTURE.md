# Codebase Structure

_Generated: 2026-04-22_

## Summary

Flat source layout with no `src/` directory — all application code lives at the project root. Components and services are in top-level subdirectories. The planned migration adds `server/`, `prisma/`, and `src/` (for auth client and AuthGuard) as new top-level directories.

## Directory Layout

```
smart-tech-termo/
├── App.tsx                  # Root component — all editor logic and state
├── index.tsx                # React mount point
├── index.html               # HTML shell, loads html2pdf via CDN
├── types.ts                 # Shared TypeScript interfaces
├── package.json             # Dependencies and scripts
├── tsconfig.json            # TypeScript config
├── vite.config.ts           # Vite build config
├── components/
│   ├── ReportPreview.tsx    # A4 document layout (print/PDF view)
│   └── SmartTechLogo.tsx    # Company SVG logo
├── services/
│   └── gemini.ts            # Gemini AI API calls (improveDescription, generateSummary)
├── .planning/
│   ├── config.json
│   ├── MILESTONES.md
│   ├── PROJECT.md
│   ├── REQUIREMENTS.md
│   ├── ROADMAP.md
│   ├── STATE.md
│   └── codebase/            # Codebase maps (this directory)
└── migrated_prompt_history/ # Legacy prompt history (not application code)
```

## Directory Purposes

**`components/`:**
- Purpose: Presentational React components
- Contains: `ReportPreview.tsx` (the printable document), `SmartTechLogo.tsx` (SVG)
- Key files: `ReportPreview.tsx` — receives `ReportData` prop and renders the full A4 formal document

**`services/`:**
- Purpose: External API integrations
- Contains: `gemini.ts` — wraps `@google/genai` SDK for text improvement and summary generation
- Note: Currently dead code — no call sites in `App.tsx`

**`.planning/`:**
- Purpose: GSD planning documents, not application code
- Generated: Partially (codebase maps are generated)
- Committed: Yes

## Key File Locations

**Entry Points:**
- `index.html`: HTML shell, imports Tailwind CDN, loads `html2pdf` CDN script, mounts to `<div id="root">`
- `index.tsx`: Creates React root and renders `<App />`
- `App.tsx`: Entire application — editor form, navigation, save/load logic, PDF trigger

**Data Model:**
- `types.ts`: `ReportData`, `LicenseItem`, `Entity` — the canonical data shape used everywhere

**Configuration:**
- `vite.config.ts`: Vite + React plugin setup
- `tsconfig.json`: TypeScript compiler options
- `package.json`: Dependencies (`react@19`, `@google/genai`, `@heroicons/react`)

**Document Rendering:**
- `components/ReportPreview.tsx`: Full A4 document layout. Props: `{ data: ReportData }`. Renders header, parties, license table, conformity declaration, installation data, credentials block, and signature field.

## Naming Conventions

**Files:**
- Components: PascalCase — `ReportPreview.tsx`, `SmartTechLogo.tsx`, `App.tsx`
- Services: camelCase — `gemini.ts`
- Types: camelCase — `types.ts`
- Entry point: camelCase — `index.tsx`

**Directories:**
- All lowercase — `components/`, `services/`

**React components:**
- Named exports for sub-components defined inline within `App.tsx` (e.g., `NavItem`, `Input`)
- Default exports for file-level components

## Where to Add New Code

**New editor section/tab:**
- Add tab button in `App.tsx` sidebar `<nav>` block
- Add conditional render block `{activeTab === 'TabName' && (...)}` in `<main>`

**New presentational component:**
- `components/NewComponent.tsx` — default export, receives typed props

**New external service:**
- `services/newService.ts` — named exports for each operation

**Backend routes (planned):**
- `server/routes/newRoute.ts`

**Auth/frontend utilities (planned):**
- `src/lib/` for auth client and shared utilities
- `src/components/` for auth-aware components like `AuthGuard.tsx`

**Data model changes:**
- Update `types.ts` interfaces first, then update `App.tsx` `initialData`, `ReportPreview.tsx` rendering, and (planned) `prisma/schema.prisma`

## Planned New Directories (from CLAUDE.md)

**`server/`:**
- Purpose: Hono + Node.js backend
- Will contain: `index.ts`, `auth.ts`, `prisma.ts`, `routes/terms.ts`, `routes/gemini.ts`
- Generated: No
- Committed: Yes

**`prisma/`:**
- Purpose: Database schema and migrations
- Will contain: `schema.prisma`, `migrations/`
- Generated: Partially (migrations are generated by Prisma)
- Committed: Yes

**`src/`:**
- Purpose: Frontend modules that depend on backend (auth client, guards)
- Will contain: `lib/auth-client.ts`, `components/AuthGuard.tsx`
- Generated: No
- Committed: Yes

## Special Files

**`index.html`:**
- Loads Tailwind via CDN play script
- Loads `html2pdf.bundle.min.js` via CDN (not in `package.json`)
- Sets `<title>` and root div

**`migrated_prompt_history/`:**
- Contains JSON prompt history from a previous AI-assisted session
- Not application code — safe to ignore

## Gaps / Unknowns

- No `src/` directory currently exists — all source is at project root, which is unconventional for Vite projects
- `vite.config.ts` not read — path aliases (if any) are unknown
- No linting config (no `.eslintrc`, no `biome.json`) — only `tsc --noEmit` as the lint script
- `components/SmartTechLogo.tsx` not read — assumed to be a static SVG wrapper
