---
phase: 07-ux-improvements
plan: 02
type: execute
wave: 2
depends_on:
  - 07-01
files_modified:
  - components/AuthGuard.tsx
autonomous: true
requirements: []

must_haves:
  truths:
    - "Botão Google exibe o logo 'G' colorido (4 cores: vermelho, azul, amarelo, verde) à esquerda do texto"
    - "Botão Google tem fundo branco (bg-white) sobre o card escuro #111111"
    - "Separador 'ou' aparece entre o formulário email/senha e o botão Google"
    - "Botão Google tem aria-label='Entrar com Google'"
    - "Nenhuma imagem externa ou src remoto é usada para o logo Google"
  artifacts:
    - path: "components/AuthGuard.tsx"
      provides: "Google OAuth button with inline SVG branding and 'ou' separator"
      contains: "aria-label=\"Entrar com Google\""
  key_links:
    - from: "components/AuthGuard.tsx Google button"
      to: "handleGoogle()"
      via: "onClick handler (unchanged)"
      pattern: "onClick={handleGoogle}"
---

<objective>
Add the official Google "G" inline SVG logo to the Google OAuth button, insert the "ou" separator between the email form and the Google button, and add aria-label for accessibility.

Purpose: The Google button currently has no visual branding — users may not recognize it as a Google login option. This plan makes it unambiguous with the standard 4-color Google "G" mark.
Output: components/AuthGuard.tsx with Google button redesigned per UI-SPEC, separator added, aria-label set.
</objective>

<execution_context>
@/home/alex/projects/smart-tech-termo/.planning/phases/07-ux-improvements/07-UI-SPEC.md
</execution_context>

<context>
@/home/alex/projects/smart-tech-termo/.planning/ROADMAP.md
@/home/alex/projects/smart-tech-termo/.planning/STATE.md

<!-- Read 07-01-SUMMARY.md once available to confirm dark theme was applied -->

<interfaces>
<!-- After 07-01, the Google button in AuthGuard.tsx looks like this: -->
<!--
  <button
    onClick={handleGoogle}
    className="mt-3 w-full bg-white border border-white/20 text-gray-700 py-3 rounded-xl text-sm font-black flex items-center justify-center gap-3 hover:bg-gray-100"
  >
    Entrar com Google
  </button>
-->
<!-- The separator and aria-label are not yet present. -->
<!-- The form closing </form> tag is followed immediately by this button. -->
<!-- handleGoogle() already calls authClient.signIn.social({ provider: 'google', callbackURL: '/' }) -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Google SVG, "ou" separator, and aria-label to Google button</name>
  <files>components/AuthGuard.tsx</files>
  <read_first>
    - components/AuthGuard.tsx (full file — verify 07-01 dark theme is applied before proceeding)
    - .planning/phases/07-ux-improvements/07-UI-SPEC.md (Google button spec, separator spec)
    - .planning/phases/07-ux-improvements/07-01-SUMMARY.md (confirm 07-01 is complete)
  </read_first>
  <action>
Make the following two changes to components/AuthGuard.tsx. Do NOT touch anything else.

**Change 1 — Add "ou" separator between `</form>` and the Google button.**

Locate the closing `</form>` tag (end of the email/password form). Immediately after `</form>`, insert this exact block:

```tsx
<div className="relative my-4">
  <div className="border-t border-white/10" />
  <span className="absolute left-1/2 -translate-x-1/2 -top-2.5 bg-[#111111] px-2 text-white/30 text-xs">ou</span>
</div>
```

**Change 2 — Replace the Google button's content and add aria-label.**

Find the existing Google button element. It currently looks like:
```tsx
<button
  onClick={handleGoogle}
  className="mt-3 w-full bg-white border border-white/20 text-gray-700 py-3 rounded-xl text-sm font-black flex items-center justify-center gap-3 hover:bg-gray-100"
>
  Entrar com Google
</button>
```

Replace it with:
```tsx
<button
  onClick={handleGoogle}
  aria-label="Entrar com Google"
  className="mt-3 w-full bg-white border border-white/20 text-gray-700 py-3 rounded-xl text-sm font-black flex items-center justify-center gap-3 hover:bg-gray-100 active:scale-95 transition-transform"
>
  <svg width="16" height="16" viewBox="0 0 24 24" aria-hidden="true">
    <path d="M22.56 12.25c0-.78-.07-1.53-.2-2.25H12v4.26h5.92c-.26 1.37-1.04 2.53-2.21 3.31v2.77h3.57c2.08-1.92 3.28-4.74 3.28-8.09z" fill="#4285F4"/>
    <path d="M12 23c2.97 0 5.46-.98 7.28-2.66l-3.57-2.77c-.98.66-2.23 1.06-3.71 1.06-2.86 0-5.29-1.93-6.16-4.53H2.18v2.84C3.99 20.53 7.7 23 12 23z" fill="#34A853"/>
    <path d="M5.84 14.09c-.22-.66-.35-1.36-.35-2.09s.13-1.43.35-2.09V7.07H2.18C1.43 8.55 1 10.22 1 12s.43 3.45 1.18 4.93l3.66-2.84z" fill="#FBBC05"/>
    <path d="M12 5.38c1.62 0 3.06.56 4.21 1.64l3.15-3.15C17.45 2.09 14.97 1 12 1 7.7 1 3.99 3.47 2.18 7.07l3.66 2.84c.87-2.6 3.3-4.53 6.16-4.53z" fill="#EA4335"/>
  </svg>
  Entrar com Google
</button>
```

Do NOT change: handleGoogle function, any other button, any form elements, any state, any other className.
  </action>
  <verify>
    <automated>
grep -n 'aria-label="Entrar com Google"' /home/alex/projects/smart-tech-termo/components/AuthGuard.tsx
grep -n 'border-t border-white/10' /home/alex/projects/smart-tech-termo/components/AuthGuard.tsx
grep -n '#4285F4\|#34A853\|#FBBC05\|#EA4335' /home/alex/projects/smart-tech-termo/components/AuthGuard.tsx
grep -n 'active:scale-95' /home/alex/projects/smart-tech-termo/components/AuthGuard.tsx
</automated>
    <automated>grep -n 'src=\|img ' /home/alex/projects/smart-tech-termo/components/AuthGuard.tsx || echo "CLEAN — no external image references"</automated>
  </verify>
  <acceptance_criteria>
    - `grep 'aria-label="Entrar com Google"' components/AuthGuard.tsx` returns exactly 1 match
    - `grep 'border-t border-white/10' components/AuthGuard.tsx` returns exactly 1 match (separator)
    - `grep '#4285F4' components/AuthGuard.tsx` returns 1 match (Google blue path)
    - `grep '#34A853' components/AuthGuard.tsx` returns 1 match (Google green path)
    - `grep '#FBBC05' components/AuthGuard.tsx` returns 1 match (Google yellow path)
    - `grep '#EA4335' components/AuthGuard.tsx` returns 1 match (Google red path)
    - `grep 'viewBox="0 0 24 24"' components/AuthGuard.tsx` returns 1 match (SVG element)
    - `grep 'src=' components/AuthGuard.tsx` returns NO match (no external image)
    - `grep 'active:scale-95' components/AuthGuard.tsx` returns 1 match (Google button active state)
    - `grep 'translate-x-1/2.*bg-\[#111111\]' components/AuthGuard.tsx` returns 1 match (separator label)
  </acceptance_criteria>
  <done>Google button shows 4-color inline SVG logo, white button style, aria-label set, "ou" separator present between form and Google button. No external image references. handleGoogle() logic unchanged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → AuthGuard UI | Visual-only; no auth logic modified |
| Browser → Google OAuth | handleGoogle() already existed; no new data flow added |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-02-01 | Spoofing | Google SVG icon | accept | Inline SVG uses standard Google "G" paths — no external fetch, no CDN dependency, cannot be hijacked via supply chain. |
| T-07-02-02 | Information Disclosure | Google OAuth flow | accept | callbackURL remains '/' (same origin). No token or secret exposed in client code. OAuth flow handled by Better Auth server-side. |
</threat_model>

<verification>
Full button integrity check — confirms SVG is well-formed and button structure is preserved:

```bash
grep -A 15 'aria-label="Entrar com Google"' components/AuthGuard.tsx
```

Should show: button open tag with aria-label, SVG with 4 path elements, "Entrar com Google" text, button close tag.
</verification>

<success_criteria>
- Google button contains inline 4-color SVG "G" logo (no external img)
- "ou" separator appears between </form> and the Google button
- aria-label="Entrar com Google" declared on the button element
- active:scale-95 transition on Google button
- handleGoogle() function call preserved (onClick unchanged)
</success_criteria>

<output>
After completion, create `.planning/phases/07-ux-improvements/07-02-SUMMARY.md`
</output>
