---
phase: 07-ux-improvements
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - components/AuthGuard.tsx
autonomous: true
requirements: []

must_haves:
  truths:
    - "Tela de login tem fundo escuro (#080808) — texto legível em todos os elementos"
    - "Card de login usa bg-[#111111] com borda border-white/10 e sombra shadow-2xl"
    - "Inputs têm fundo bg-black/40, borda border-white/10 e texto text-white"
    - "Botão de submit usa bg-red-600 (não bg-blue-600)"
    - "Estado de carregamento (isPending) usa fundo #080808 e texto text-white/40"
    - "Texto auxiliar e links usam paleta escura (text-white/40, text-red-500)"
  artifacts:
    - path: "components/AuthGuard.tsx"
      provides: "Login screen dark theme"
      contains: "bg-[#080808]"
  key_links:
    - from: "components/AuthGuard.tsx"
      to: "dark color palette"
      via: "inline Tailwind classes"
      pattern: "bg-\\[#080808\\]"
---

<objective>
Apply the dark theme to AuthGuard.tsx: page background, card, inputs, submit button, loading state, error text, and helper text/links.

Purpose: The login screen currently uses light theme (bg-gray-50/white card) which is visually broken against the rest of the dark app. This plan aligns it to the app palette.
Output: components/AuthGuard.tsx with full dark theme. Google button wrapper updated but no SVG yet (that is plan 07-02).
</objective>

<execution_context>
@/home/alex/projects/smart-tech-termo/.planning/phases/07-ux-improvements/07-UI-SPEC.md
</execution_context>

<context>
@/home/alex/projects/smart-tech-termo/.planning/ROADMAP.md
@/home/alex/projects/smart-tech-termo/.planning/STATE.md

<interfaces>
<!-- Current AuthGuard.tsx structure (read from source) -->
<!-- Page wrapper: <div className="min-h-screen flex items-center justify-center bg-gray-50"> -->
<!-- Card: <div className="bg-white p-8 rounded shadow-md w-full max-w-sm"> -->
<!-- Title: <h1 className="text-xl font-semibold mb-6 text-center"> -->
<!-- Name input className: "w-full border rounded px-3 py-2 text-sm" -->
<!-- Email input className: "w-full border rounded px-3 py-2 text-sm" -->
<!-- Password input className: "w-full border rounded px-3 py-2 text-sm" -->
<!-- Error: <p className="text-red-500 text-sm"> -->
<!-- Submit button: className="w-full bg-blue-600 text-white py-2 rounded text-sm font-medium hover:bg-blue-700 disabled:opacity-50" -->
<!-- Google button (keep existing markup, only update outer className): className="mt-3 w-full border py-2 rounded text-sm hover:bg-gray-50" -->
<!-- Loading state: <div className="min-h-screen flex items-center justify-center"> / <div className="text-gray-500"> -->
<!-- Helper text: <p className="mt-4 text-center text-sm text-gray-500"> -->
<!-- Link buttons: className="text-blue-600 hover:underline" -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Apply dark theme to AuthGuard.tsx — page, card, inputs, buttons, states</name>
  <files>components/AuthGuard.tsx</files>
  <read_first>
    - components/AuthGuard.tsx (full file — current state before any edits)
    - .planning/phases/07-ux-improvements/07-UI-SPEC.md (authoritative color/typography spec)
  </read_first>
  <action>
Apply ALL of the following class replacements in components/AuthGuard.tsx. Make every change exactly — do not add, remove, or reword anything beyond what is listed here.

**Loading state (isPending block, lines ~17-23):**
- Page wrapper div: `className="min-h-screen flex items-center justify-center"` → `className="min-h-screen flex items-center justify-center bg-[#080808]"`
- Loading text div: `className="text-gray-500"` → `className="text-white/40"`

**Page wrapper (main return, unauthenticated branch):**
- `className="min-h-screen flex items-center justify-center bg-gray-50"` → `className="min-h-screen flex items-center justify-center bg-[#080808]"`

**Card div:**
- `className="bg-white p-8 rounded shadow-md w-full max-w-sm"` → `className="bg-[#111111] p-8 rounded-2xl shadow-2xl border border-white/10 w-full max-w-sm"`

**Title h1:**
- `className="text-xl font-semibold mb-6 text-center"` → `className="text-xl font-black mb-6 text-center text-white"`

**Name input (inside mode === 'signup' conditional):**
- `className="w-full border rounded px-3 py-2 text-sm"` → `className="w-full bg-black/40 border border-white/10 rounded-xl px-4 py-3 text-sm text-white placeholder:text-white/30 focus:outline-none focus:border-red-600"`

**Email input:**
- `className="w-full border rounded px-3 py-2 text-sm"` → `className="w-full bg-black/40 border border-white/10 rounded-xl px-4 py-3 text-sm text-white placeholder:text-white/30 focus:outline-none focus:border-red-600"`

**Password input:**
- `className="w-full border rounded px-3 py-2 text-sm"` → `className="w-full bg-black/40 border border-white/10 rounded-xl px-4 py-3 text-sm text-white placeholder:text-white/30 focus:outline-none focus:border-red-600"`

**Error paragraph:**
- `className="text-red-500 text-sm"` → `className="text-red-400 text-sm"`

**Submit button:**
- `className="w-full bg-blue-600 text-white py-2 rounded text-sm font-medium hover:bg-blue-700 disabled:opacity-50"` → `className="w-full bg-red-600 hover:bg-red-500 text-white py-3 rounded-xl text-sm font-black disabled:opacity-50"`

**Google button (outer wrapper only — keep inner text "Entrar com Google" unchanged for now):**
- `className="mt-3 w-full border py-2 rounded text-sm hover:bg-gray-50"` → `className="mt-3 w-full bg-white border border-white/20 text-gray-700 py-3 rounded-xl text-sm font-black flex items-center justify-center gap-3 hover:bg-gray-100"`

**Helper text paragraph:**
- `className="mt-4 text-center text-sm text-gray-500"` → `className="mt-4 text-center text-sm text-white/40"`

**Toggle link buttons (both "Criar conta" and "Entrar" links inside the helper paragraph):**
- `className="text-blue-600 hover:underline"` → `className="text-red-500 hover:text-red-400 hover:underline"`

Do NOT change: function logic, state variables, form onSubmit, handleGoogle, mode state, or any markup structure.
  </action>
  <verify>
    <automated>
grep -n 'bg-\[#080808\]' /home/alex/projects/smart-tech-termo/components/AuthGuard.tsx
grep -n 'bg-\[#111111\]' /home/alex/projects/smart-tech-termo/components/AuthGuard.tsx
grep -n 'bg-red-600' /home/alex/projects/smart-tech-termo/components/AuthGuard.tsx
grep -n 'text-white/40' /home/alex/projects/smart-tech-termo/components/AuthGuard.tsx
grep -n 'text-red-500 hover:text-red-400' /home/alex/projects/smart-tech-termo/components/AuthGuard.tsx
</automated>
    <automated>grep -n 'bg-gray-50\|bg-white\|bg-blue-600\|text-gray-500\|text-blue-600\|font-semibold\|font-medium' /home/alex/projects/smart-tech-termo/components/AuthGuard.tsx || echo "CLEAN — no broken classes remaining"</automated>
  </verify>
  <acceptance_criteria>
    - `grep 'bg-\[#080808\]' components/AuthGuard.tsx` returns at least 2 matches (page wrapper + loading state)
    - `grep 'bg-\[#111111\]' components/AuthGuard.tsx` returns exactly 1 match (card)
    - `grep 'bg-blue-600' components/AuthGuard.tsx` returns NO match (old submit button color gone)
    - `grep 'bg-gray-50' components/AuthGuard.tsx` returns NO match (old page bg gone)
    - `grep 'bg-white' components/AuthGuard.tsx` returns exactly 1 match (Google button — intentional white bg)
    - `grep 'text-gray-500' components/AuthGuard.tsx` returns NO match
    - `grep 'text-blue-600' components/AuthGuard.tsx` returns NO match
    - `grep 'font-semibold\|font-medium' components/AuthGuard.tsx` returns NO match
    - `grep 'bg-red-600' components/AuthGuard.tsx` returns 1 match (submit button)
    - `grep 'text-red-400' components/AuthGuard.tsx` returns 1 match (error text)
    - `grep 'text-red-500 hover:text-red-400' components/AuthGuard.tsx` returns 2 matches (both toggle link buttons)
    - `grep 'focus:border-red-600' components/AuthGuard.tsx` returns 3 matches (name, email, password inputs)
  </acceptance_criteria>
  <done>AuthGuard.tsx uses full dark theme. All light-theme classes eliminated. Submit button is red-600. Loading state uses dark bg. Helper links are red-500. Zero blue-600 or gray-50 references remain.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → AuthGuard UI | Visual-only changes; no auth logic modified |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-01-01 | Spoofing | AuthGuard.tsx | accept | No auth logic changed; only CSS classes modified. Existing Better Auth session validation unchanged. |
| T-07-01-02 | Information Disclosure | AuthGuard.tsx | accept | No new data flows introduced. Color changes do not expose credentials or session data. |
</threat_model>

<verification>
After the task completes, verify the file renders correctly by checking the diff for unexpected structural changes:

```bash
grep -c 'className=' components/AuthGuard.tsx
```

The count should match the original file's className count (no elements added or removed — only class values changed).
</verification>

<success_criteria>
- Dark theme applied to all elements in AuthGuard.tsx
- Zero legacy light-theme classes remain (bg-gray-50, bg-white card, bg-blue-600, text-gray-500, text-blue-600, font-semibold, font-medium)
- Submit button is bg-red-600, inputs have focus:border-red-600
- Loading state uses bg-[#080808] background
</success_criteria>

<output>
After completion, create `.planning/phases/07-ux-improvements/07-01-SUMMARY.md`
</output>
