---
phase: 01-backend-foundation
plan: 03
type: execute
wave: 3
depends_on:
  - 01-01
  - 01-02
files_modified:
  - prisma/migrations/
autonomous: false
requirements:
  - BACK-01
must_haves:
  truths:
    - "npx prisma migrate dev aplica o schema sem erros"
    - "PrismaClient conecta ao banco PostgreSQL local"
    - "npx tsx server/index.ts inicia sem erros"
    - "GET /api/health retorna HTTP 200"
  artifacts:
    - path: "prisma/migrations/"
      provides: "Histórico de migrações aplicadas"
      contains: "migration.sql"
    - path: "node_modules/.prisma/client/"
      provides: "PrismaClient gerado a partir do schema"
  key_links:
    - from: "prisma/migrations/"
      to: "PostgreSQL (local)"
      via: "npx prisma migrate dev"
      pattern: "CREATE TABLE.*terms"
    - from: "server/index.ts"
      to: "GET /api/health"
      via: "curl localhost:3001"
      pattern: "status.*ok"
---

<objective>
[BLOCKING] Rodar a migração do banco de dados e verificar que o servidor Hono sobe corretamente com conexão ao PostgreSQL.

Purpose: Sem a migração aplicada, o servidor não pode operar — qualquer query ao Prisma falharia. Este plano confirma que toda a infraestrutura da Fase 1 funciona de ponta a ponta.
Output: Tabela "terms" criada no PostgreSQL; PrismaClient gerado; servidor respondendo em /api/health.
</objective>

<execution_context>
@/home/alex/.claude/get-shit-done/workflows/execute-plan.md
@/home/alex/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@CLAUDE.md

@.planning/phases/01-backend-foundation/01-01-SUMMARY.md
@.planning/phases/01-backend-foundation/01-02-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: [PRÉ-REQUISITO] Garantir que o PostgreSQL local está rodando</name>
  <read_first>
    - /home/alex/projects/smart-tech-termo/CLAUDE.md — seção "Como rodar localmente" e seção "Docker Compose" (para confirmar nome do serviço postgres)
    - /home/alex/projects/smart-tech-termo/.env — confirmar que DATABASE_URL aponta para localhost:5432
  </read_first>
  <what-to-check>
O PostgreSQL local DEVE estar rodando antes de continuar. Não é possível aplicar a migração sem banco disponível.

Verifique se o container postgres já está ativo:

```bash
docker compose ps
```

Se o serviço `postgres` não estiver rodando (status "Up"), inicie-o:

```bash
docker compose up postgres -d
```

Aguarde alguns segundos e confirme que está acessível:

```bash
docker compose ps postgres
# Deve mostrar status "Up" ou "healthy"
```

Confirme também que o .env tem o DATABASE_URL correto para dev local:
```
DATABASE_URL=postgresql://termo:termo@localhost:5432/termo
```

Se o PostgreSQL não estiver configurado via Docker ainda, é necessário ter um PostgreSQL local acessível com as credenciais do .env.
  </what-to-check>
  <resume-signal>Digite "postgres ok" quando o banco estiver acessível e o DATABASE_URL estiver correto no .env</resume-signal>
</task>

<task type="auto">
  <name>Task 2: [BLOCKING] Rodar migrate dev e gerar PrismaClient</name>
  <read_first>
    - /home/alex/projects/smart-tech-termo/prisma/schema.prisma — confirmar schema antes de migrar
    - /home/alex/projects/smart-tech-termo/.env — confirmar DATABASE_URL antes de rodar o comando
  </read_first>
  <files>prisma/migrations/</files>
  <action>
Com o PostgreSQL rodando e o DATABASE_URL configurado no .env, executar em sequência:

```bash
# 1. Aplicar migração (cria a tabela "terms" no banco)
npx prisma migrate dev --name init

# 2. Gerar PrismaClient (deve acontecer automaticamente após migrate, mas garantir)
npx prisma generate
```

O comando `npx prisma migrate dev --name init` é INTERATIVO — pode perguntar se deseja criar o banco de dados caso ele não exista. Responder "y" (yes).

Se o migrate falhar com "Can't reach database server":
- Verificar que o PostgreSQL está rodando: `docker compose ps postgres`
- Verificar que o DATABASE_URL no .env aponta para o host correto
- Se usando Docker, confirmar que a porta 5432 está mapeada: `docker compose ps`

Se o migrate falhar com "Database 'termo' does not exist":
- Rodar: `docker compose exec postgres psql -U termo -c "CREATE DATABASE termo;"` e repetir o migrate
  </action>
  <verify>
    <automated>test -d prisma/migrations && ls prisma/migrations/ | grep -q "init" && echo "migrations OK" && npx prisma validate && echo "schema OK"</automated>
  </verify>
  <acceptance_criteria>
    - `npx prisma migrate dev --name init` termina com saída "Your database is now in sync with your schema"
    - Diretório prisma/migrations/ existe com pelo menos uma pasta contendo migration.sql
    - O migration.sql contém "CREATE TABLE" para a tabela "terms"
    - `npx prisma validate` retorna sem erros
    - `npx prisma generate` completa sem erros e gera node_modules/.prisma/client/
    - Não há erros de conexão ao banco durante o migrate
  </acceptance_criteria>
  <done>Migração aplicada com sucesso: tabela "terms" existe no PostgreSQL e PrismaClient gerado.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verificar servidor Hono respondendo em /api/health</name>
  <read_first>
    - /home/alex/projects/smart-tech-termo/server/index.ts — confirmar porta e rota configuradas
  </read_first>
  <what-built>
O servidor Hono foi criado em server/index.ts com rota GET /api/health. Agora é necessário confirmar que sobe sem erros e responde corretamente.

Em um terminal separado, inicie o servidor:

```bash
npx tsx server/index.ts
```

Deve imprimir: `Server running on http://localhost:3001`

Em outro terminal, testar o healthcheck:

```bash
curl -s http://localhost:3001/api/health
```

Resposta esperada:
```json
{"status":"ok","timestamp":"2026-04-22T..."}
```

Testar também que o status HTTP é 200:
```bash
curl -s -o /dev/null -w "%{http_code}" http://localhost:3001/api/health
# Deve retornar: 200
```
  </what-built>
  <how-to-verify>
    1. Rodar `npx tsx server/index.ts` no terminal
    2. Confirmar que imprime "Server running on http://localhost:3001" sem erros
    3. Rodar `curl -s http://localhost:3001/api/health` em outro terminal
    4. Confirmar que a resposta JSON contém `"status":"ok"`
    5. Confirmar que o status HTTP é 200
  </how-to-verify>
  <resume-signal>Digite "health ok" se o servidor respondeu corretamente, ou descreva o erro observado</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Descrição |
|----------|-----------|
| migrate dev → PostgreSQL | Comando usa DATABASE_URL do .env — nunca credenciais hardcodadas |
| GET /api/health → cliente | Resposta não pode expor informações do banco ou do sistema |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-09 | Information Disclosure | GET /api/health — resposta | mitigate | Resposta contém apenas `{ status, timestamp }` — sem versão do Prisma, sem DATABASE_URL, sem stack traces |
| T-01-10 | Information Disclosure | npx prisma migrate dev — logs | accept | Logs de migração são exibidos no terminal do desenvolvedor apenas; não expostos a clientes externos |
| T-01-11 | Denial of Service | PostgreSQL — conexão | accept | Ambiente de desenvolvimento local; PrismaClient usa connection pool padrão do Prisma (limite razoável para dev) |
| T-01-12 | Information Disclosure | prisma/migrations/*.sql | accept | Arquivos de migração ficam no repositório; não contêm dados de usuários, apenas estrutura de tabelas |
</threat_model>

<verification>
Verificação completa da Fase 1:

```bash
# 1. Schema válido
npx prisma validate
# Esperado: "The schema at prisma/schema.prisma is valid"

# 2. Migrações aplicadas
ls prisma/migrations/
# Deve listar pasta com timestamp e "init"

# 3. Tabela criada no banco
docker compose exec postgres psql -U termo -d termo -c "\dt"
# Deve listar a tabela "terms"

# 4. Servidor inicia (teste com timeout)
timeout 5 npx tsx server/index.ts & sleep 3 && curl -s http://localhost:3001/api/health && kill %1
# Deve retornar JSON com "status":"ok"
```

Critérios de sucesso da Fase 1 (do ROADMAP.md):
1. `npx tsx watch server/index.ts` inicia sem erros ✓
2. `GET /api/health` retorna 200 ✓
3. `npx prisma migrate dev` aplica o schema sem erros ✓
4. PrismaClient conecta ao banco PostgreSQL local ✓
</verification>

<success_criteria>
- `npx prisma validate` passa sem erros
- `npx prisma migrate dev` completa com "Your database is now in sync with your schema"
- Tabela "terms" existe no PostgreSQL (confirmada via psql ou pg_tables)
- `curl http://localhost:3001/api/health` retorna 200 com `{"status":"ok",...}`
- Servidor não loga nenhum segredo no console
</success_criteria>

<output>
Após conclusão, criar `.planning/phases/01-backend-foundation/01-03-SUMMARY.md` com:
- Status da migração (nome, timestamp)
- Confirmação de que o servidor respondeu em /api/health
- Quaisquer problemas encontrados durante o migrate e como foram resolvidos
- Fase 1 marcada como completa
</output>
