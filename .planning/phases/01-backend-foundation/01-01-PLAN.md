---
phase: 01-backend-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - .env
  - .gitignore
autonomous: true
requirements:
  - BACK-01
must_haves:
  truths:
    - "Todos os pacotes do servidor estão instalados em node_modules"
    - "package.json contém script dev:server e mantém os scripts existentes"
    - ".env existe com as variáveis de ambiente necessárias"
    - ".gitignore exclui .env do controle de versão"
  artifacts:
    - path: "package.json"
      provides: "Dependências do servidor declaradas"
      contains: "hono"
    - path: ".env"
      provides: "Variáveis de ambiente do servidor"
      contains: "DATABASE_URL"
    - path: ".gitignore"
      provides: "Proteção do .env"
      contains: ".env"
  key_links:
    - from: "package.json scripts.dev:server"
      to: "server/index.ts"
      via: "tsx watch"
      pattern: "tsx watch server/index.ts"
---

<objective>
Instalar todas as dependências do servidor e preparar o ambiente local (scripts, .env, .gitignore).

Purpose: Sem esses pacotes instalados, nenhum arquivo de servidor pode ser importado ou executado. Este plano cria a fundação para os planos seguintes.
Output: node_modules com hono/prisma/tsx; package.json atualizado; .env criado; .gitignore protegendo o .env.
</objective>

<execution_context>
@/home/alex/.claude/get-shit-done/workflows/execute-plan.md
@/home/alex/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Instalar dependências do servidor e atualizar scripts</name>
  <read_first>
    - /home/alex/projects/smart-tech-termo/CLAUDE.md — lista exata dos pacotes a instalar e scripts a adicionar
    - /home/alex/projects/smart-tech-termo/package.json — estado atual antes de modificar
  </read_first>
  <files>package.json</files>
  <action>
Executar na raiz do projeto:

```bash
npm install hono @hono/node-server better-auth @prisma/client @prisma/adapter-pg prisma dotenv
npm install -D tsx
```

Após a instalação, editar package.json para adicionar o script dev:server. O arquivo já tem "dev", "build", "preview", "lint" — mantê-los intactos. Adicionar apenas:

```json
"dev:server": "tsx watch server/index.ts"
```

Estado final esperado de package.json.scripts:
```json
{
  "dev": "vite",
  "build": "vite build",
  "preview": "vite preview",
  "lint": "tsc --noEmit",
  "dev:server": "tsx watch server/index.ts"
}
```
  </action>
  <verify>
    <automated>node -e "const p = JSON.parse(require('fs').readFileSync('package.json','utf8')); const deps=['hono','@hono/node-server','better-auth','@prisma/client','@prisma/adapter-pg','prisma','dotenv']; deps.forEach(d=>{if(!p.dependencies[d])throw new Error('Missing dep: '+d)}); if(!p.devDependencies['tsx'])throw new Error('Missing devDep: tsx'); if(!p.scripts['dev:server'])throw new Error('Missing script: dev:server'); console.log('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - package.json contém "hono" em dependencies
    - package.json contém "@hono/node-server" em dependencies
    - package.json contém "better-auth" em dependencies
    - package.json contém "@prisma/client" em dependencies
    - package.json contém "@prisma/adapter-pg" em dependencies
    - package.json contém "prisma" em dependencies
    - package.json contém "dotenv" em dependencies
    - package.json contém "tsx" em devDependencies
    - package.json contém "dev:server" em scripts com valor "tsx watch server/index.ts"
    - Scripts existentes (dev, build, preview, lint) permanecem inalterados
    - node_modules/hono existe após npm install
  </acceptance_criteria>
  <done>npm install completa sem erros, package.json atualizado com todas as dependências e o script dev:server.</done>
</task>

<task type="auto">
  <name>Task 2: Criar .env e proteger com .gitignore</name>
  <read_first>
    - /home/alex/projects/smart-tech-termo/CLAUDE.md — seção "Variáveis de ambiente" com os nomes exatos das variáveis
    - /home/alex/projects/smart-tech-termo/.gitignore — verificar se já existe antes de criar/modificar
  </read_first>
  <files>.env, .gitignore</files>
  <action>
Criar o arquivo `.env` na raiz do projeto com o seguinte conteúdo (valores são placeholders — o desenvolvedor preencherá com valores reais):

```
DATABASE_URL=postgresql://termo:termo@localhost:5432/termo
BETTER_AUTH_URL=http://localhost:3001
BETTER_AUTH_SECRET=changeme-run-openssl-rand-base64-32
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
GEMINI_API_KEY=
```

Em seguida, verificar se `.gitignore` existe na raiz. Se não existir, criar com o conteúdo abaixo. Se existir, adicionar `.env` ao final (se ainda não estiver presente):

```
# dependencies
node_modules/

# env
.env
.env.local
.env.*.local

# build output
dist/

# editor
.DS_Store
```

IMPORTANTE: O arquivo `.env` NÃO deve ser commitado. A presença de `.env` no `.gitignore` é mandatória.
  </action>
  <verify>
    <automated>grep -q "DATABASE_URL" .env && grep -q "BETTER_AUTH_URL" .env && grep -q "BETTER_AUTH_SECRET" .env && grep -q "GEMINI_API_KEY" .env && grep -q "\.env" .gitignore && echo "OK"</automated>
  </verify>
  <acceptance_criteria>
    - .env existe na raiz do projeto
    - .env contém linha "DATABASE_URL=postgresql://termo:termo@localhost:5432/termo"
    - .env contém linha "BETTER_AUTH_URL=http://localhost:3001"
    - .env contém linha "BETTER_AUTH_SECRET=..."
    - .env contém linha "GOOGLE_CLIENT_ID="
    - .env contém linha "GOOGLE_CLIENT_SECRET="
    - .env contém linha "GEMINI_API_KEY="
    - .gitignore contém ".env" (git status não lista .env como untracked)
    - git status não mostra .env como arquivo a ser commitado
  </acceptance_criteria>
  <done>.env criado com todas as variáveis necessárias; .gitignore impede que seja commitado acidentalmente.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Descrição |
|----------|-----------|
| filesystem → git | .env não pode vazar para o repositório |
| .env → runtime | DATABASE_URL e BETTER_AUTH_SECRET são segredos — nunca hardcodados |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Information Disclosure | .env / DATABASE_URL | mitigate | .gitignore inclui .env; verificação explícita na acceptance_criteria |
| T-01-02 | Information Disclosure | BETTER_AUTH_SECRET | mitigate | Placeholder no .env instrui o desenvolvedor a gerar com `openssl rand -base64 32`; nunca usar valor padrão em produção |
| T-01-03 | Information Disclosure | GEMINI_API_KEY | mitigate | Variável fica apenas no servidor (não exposta ao build do Vite); Fase 4 move a chamada para o backend |
| T-01-04 | Tampering | package.json scripts | accept | Ambiente de desenvolvimento interno; risco baixo |
</threat_model>

<verification>
Após concluir ambas as tarefas:

```bash
# Verificar pacotes instalados
node -e "require('hono'); console.log('hono OK')"
node -e "require('@hono/node-server'); console.log('node-server OK')"
npx prisma --version

# Verificar .env e .gitignore
grep "DATABASE_URL" .env
grep "\.env" .gitignore

# Garantir que .env não está sendo rastreado pelo git
git status | grep -v ".env" && echo "OK: .env não está sendo rastreado"
```
</verification>

<success_criteria>
- `npx prisma --version` retorna versão sem erro
- `node -e "require('hono')"` executa sem erro
- `.env` contém as 6 variáveis de ambiente necessárias
- `git status` não lista `.env` como arquivo a commitar
</success_criteria>

<output>
Após conclusão, criar `.planning/phases/01-backend-foundation/01-01-SUMMARY.md` com:
- Pacotes instalados e suas versões
- Variáveis de ambiente configuradas (sem os valores)
- Status do .gitignore
</output>
