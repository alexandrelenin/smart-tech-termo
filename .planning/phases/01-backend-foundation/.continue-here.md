---
context: phase
phase: 01-backend-foundation
task: 1
total_tasks: 3
status: in_progress
last_updated: 2026-04-22T14:33:00
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

> These are not suggestions. Each constraint below was discovered through failure.
> Acknowledge each one explicitly before proceeding.

- [ ] CONSTRAINT: Gitignored files don't survive worktree merges — `.env` must be recreated manually in the main working tree after each Wave merge.

**Do not proceed until all boxes are checked.**

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| Worktree gitignore leak | Gitignored files (`.env`) created inside a worktree are lost when the worktree is removed — they never appear in the merge because git doesn't track them | advisory | After each worktree merge, verify `.env` exists in main tree; recreate from plan spec if missing |
| Prisma 7 config syntax | Prisma 7.8.0 removed `url` from datasource block in schema.prisma — config is now in `prisma.config.ts` with `defineConfig`; `server/prisma.ts` must use `PrismaPg` driver adapter | advisory | Already fixed in Wave 2; do not revert to old schema.prisma `url = env(...)` pattern |

<current_state>
Execução da Fase 1 em andamento. Waves 1 e 2 concluídas e mergeadas para main.
Parados no início de Wave 3 (plano 01-03) aguardando checkpoint humano: o usuário precisa confirmar que o PostgreSQL está rodando antes de executar `npx prisma migrate dev`.
</current_state>

<completed_work>

Wave 1 — 01-01 (COMPLETO, commitado, mergeado):
- npm install: hono, @hono/node-server, better-auth, @prisma/client, @prisma/adapter-pg, prisma@7.8.0, dotenv (prod) + tsx (dev)
- package.json: script `dev:server` adicionado
- .gitignore: atualizado com .env, .env.local, .env.*.local, dist/, node_modules/
- .env: criado com 6 variáveis placeholder (recriado manualmente na main tree após merge)

Wave 2 — 01-02 (COMPLETO, commitado, mergeado):
- server/prisma.ts: singleton PrismaClient com PrismaPg driver adapter (Prisma 7 exige)
- prisma/schema.prisma: model Term com todos os 16 campos de ReportData (sem `url` no datasource — Prisma 7)
- prisma.config.ts: defineConfig com env('DATABASE_URL') — novo padrão Prisma 7
- server/index.ts: Hono na porta 3001, GET /api/health retorna {status:'ok', timestamp}
- `npx prisma validate` passou com sucesso
</completed_work>

<remaining_work>

Wave 3 — 01-03 (NÃO INICIADA):
- Task 1 [CHECKPOINT:human-action]: Confirmar que PostgreSQL está rodando (`docker compose up postgres -d`)
- Task 2 [auto]: Rodar `npx prisma migrate dev --name init` + `npx prisma generate`
- Task 3 [CHECKPOINT:human-verify]: Iniciar servidor e confirmar `GET /api/health` retorna 200

Após Fase 1:
- Fase 2 (ou próximas fases): Better Auth setup, rotas de termos, proxy Gemini, migração do frontend, Docker
</remaining_work>

<decisions_made>

- Prisma 7.8.0 usa `prisma.config.ts` em vez de `url` no schema.prisma — decidido pelo executor como auto-fix (Regra 1)
- server/prisma.ts usa PrismaPg driver adapter (obrigatório no Prisma 7)
- .env não é commitado (gitignored) — criado manualmente na main tree após merge da worktree
- Cada plano executado em worktree isolada, mergeado com --no-ff, STATE.md/ROADMAP.md restaurados pós-merge
</decisions_made>

<blockers>

- PostgreSQL NÃO está rodando — necessário para executar a migração Prisma
- Usuário deve preencher .env com valores reais:
  - BETTER_AUTH_SECRET: `openssl rand -base64 32`
  - GOOGLE_CLIENT_ID / GOOGLE_CLIENT_SECRET: reutilizar dos outros apps Smart Tech
  - GEMINI_API_KEY: chave existente do projeto
</blockers>

## Required Reading (in order)

1. `.planning/phases/01-backend-foundation/01-03-PLAN.md` — plano completo da Wave 3 com as 3 tasks e critérios de aceitação
2. `.planning/phases/01-backend-foundation/01-01-SUMMARY.md` — o que foi instalado (versões)
3. `.planning/phases/01-backend-foundation/01-02-SUMMARY.md` — desvio Prisma 7 e arquivos criados
4. `CLAUDE.md` — arquitetura completa do projeto

## Infrastructure State

- PostgreSQL: **NÃO rodando** — iniciar com `docker compose up postgres -d`
- node_modules: instalado (npm install executado)
- .env: existe em `/home/alex/projects/smart-tech-termo/.env` com placeholders
- server/index.ts: criado mas não testado (aguarda banco para Wave 3)
- prisma/schema.prisma: validado (`npx prisma validate` passa), migração não aplicada ainda

<context>
A execução da Fase 1 está em andamento. A estrutura completa do servidor já existe (arquivos criados e validados), faltando apenas a migração do banco e a verificação do healthcheck. O maior bloqueio é o PostgreSQL — sem ele, `npx prisma migrate dev` falhará imediatamente.

O plano 01-03 tem autonomous: false com dois checkpoints bloqueantes que requerem ação humana: confirmar que o banco está acessível e depois confirmar que o servidor responde em /api/health.
</context>

<next_action>
Start with: Iniciar o PostgreSQL com `docker compose up postgres -d`, confirmar status, então retomar a execução da Fase 1 com `/gsd-execute-phase 1` — o orquestrador detectará que os planos 01-01 e 01-02 já têm SUMMARY.md e pulará direto para o Wave 3 (01-03).
</next_action>
