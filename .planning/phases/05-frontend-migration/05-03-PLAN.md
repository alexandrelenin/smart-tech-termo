---
phase: 05-frontend-migration
plan: 03
wave: 2
autonomous: true
requirements:
  - AUTH-01
  - AUTH-02
  - AUTH-03
files_modified:
  - index.tsx
must_haves:
  truths:
    - "App é envolto por AuthGuard no ponto de entrada"
    - "Usuário não autenticado vê tela de login antes do app"
    - "Usuário autenticado vê o app normalmente"
  artifacts:
    - path: "index.tsx"
      provides: "AuthGuard ativo no entry point"
      contains: "AuthGuard"
---

<objective>
Integrar `AuthGuard` no entry point do app para que usuários não autenticados vejam a tela de login.

Purpose: O backend exige autenticação em todas as rotas. Sem AuthGuard no frontend, o app quebraria para usuários não logados tentando acessar /api/terms.
Output: index.tsx (ou App.tsx) com AuthGuard envolvendo o app. Fluxo completo: login → app → operações salvas no banco.
</objective>

<context>
@.planning/PROJECT.md
@CLAUDE.md
@.planning/phases/02-auth/02-03-SUMMARY.md
@.planning/phases/05-frontend-migration/05-01-SUMMARY.md
@.planning/phases/05-frontend-migration/05-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Integrar AuthGuard no entry point</name>
  <files>index.tsx</files>
  <action>
Ler o arquivo `index.tsx` (entry point do React).
Envolver o componente `<App />` com `<AuthGuard>`:

```tsx
import AuthGuard from './components/AuthGuard'

// ...
root.render(
  <AuthGuard>
    <App />
  </AuthGuard>
)
```

Se `index.tsx` não existir com esse nome, verificar qual é o entry point (pode ser `main.tsx`).
  </action>
  <verify>
    <automated>grep -l "AuthGuard" index.tsx main.tsx 2>/dev/null | head -1 | xargs -I{} echo "AuthGuard in {}"</automated>
  </verify>
  <acceptance_criteria>
    - AuthGuard envolve App no entry point
    - `npx tsc --noEmit` passa sem erros
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Verificar build sem erros TypeScript</name>
  <action>
Rodar `npx tsc --noEmit` para confirmar que todas as alterações da Fase 5 compilam sem erros.
Corrigir quaisquer erros de tipo encontrados.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | wc -l | xargs -I{} test {} -eq 0 && echo "TS OK" || npx tsc --noEmit 2>&1</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` retorna sem erros
    - AuthGuard está integrado no entry point
  </acceptance_criteria>
</task>

</tasks>

<success_criteria>
- AuthGuard integrado no entry point
- `npx tsc --noEmit` sem erros
- Fase 5 completa: frontend migrado de localStorage para API, Gemini via proxy, autenticação ativa
</success_criteria>

<output>
Criar `.planning/phases/05-frontend-migration/05-03-SUMMARY.md` após conclusão.
</output>
