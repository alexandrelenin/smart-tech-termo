---
phase: 05-frontend-migration
plan: 02
wave: 1
autonomous: true
requirements:
  - GEM-01
  - GEM-02
files_modified:
  - App.tsx
  - services/gemini.ts
must_haves:
  truths:
    - "App.tsx não importa services/gemini.ts"
    - "Chamadas Gemini usam fetch('/api/gemini/improve') e fetch('/api/gemini/summary')"
    - "GEMINI_API_KEY não aparece em nenhum arquivo do frontend"
  artifacts:
    - path: "App.tsx"
      provides: "Chamadas Gemini via proxy backend"
      contains: "fetch('/api/gemini"
---

<objective>
Substituir as chamadas diretas a `services/gemini.ts` em `App.tsx` por chamadas ao proxy backend `/api/gemini/*`. Remover `services/gemini.ts`.

Purpose: A chave GEMINI_API_KEY não deve ser exposta no bundle do frontend. O proxy backend já está pronto em `/api/gemini/improve` e `/api/gemini/summary`.
Output: App.tsx usa o proxy backend para Gemini. Arquivo services/gemini.ts removido.
</objective>

<context>
@.planning/PROJECT.md
@CLAUDE.md
@.planning/phases/04-gemini-proxy/04-01-SUMMARY.md
@.planning/phases/05-frontend-migration/05-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Substituir chamadas Gemini em App.tsx</name>
  <files>App.tsx</files>
  <action>
Em App.tsx, substituir:
- `improveDescription(text, type)` → `fetch('/api/gemini/improve', { method: 'POST', headers: {'Content-Type': 'application/json'}, body: JSON.stringify({ text, type }) }).then(r => r.json()).then(d => d.result)`
- `generateSummary(items)` → `fetch('/api/gemini/summary', { method: 'POST', headers: {'Content-Type': 'application/json'}, body: JSON.stringify({ items }) }).then(r => r.json()).then(d => d.result)`

Remover o import de `services/gemini.ts` no topo de App.tsx.
  </action>
</task>

<task type="auto">
  <name>Task 2: Remover services/gemini.ts</name>
  <files>services/gemini.ts</files>
  <action>
Deletar o arquivo `services/gemini.ts` — a lógica foi movida para `server/routes/gemini.ts`.
Verificar que nenhum outro arquivo importa `services/gemini.ts`.
  </action>
  <verify>
    <automated>! test -f services/gemini.ts && echo "gemini.ts removed" || echo "gemini.ts still exists"</automated>
  </verify>
  <acceptance_criteria>
    - `services/gemini.ts` não existe mais
    - `grep -r "services/gemini" . --include="*.ts" --include="*.tsx" | grep -v node_modules` retorna vazio
    - App.tsx usa fetch para /api/gemini/improve e /api/gemini/summary
  </acceptance_criteria>
</task>

</tasks>

<success_criteria>
- `services/gemini.ts` deletado
- Nenhum arquivo frontend importa `@google/genai` diretamente
- Chamadas Gemini em App.tsx usam fetch('/api/gemini/...')
</success_criteria>

<output>
Criar `.planning/phases/05-frontend-migration/05-02-SUMMARY.md` após conclusão.
</output>
