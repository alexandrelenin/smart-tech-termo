# Milestone v1.0: Backend + Auth + Persistência

**Status:** ✅ SHIPPED 2026-05-07
**Phases:** 1-7
**Total Plans:** 19
**Duration:** 2026-04-22 → 2026-05-07 (~16 dias)
**Stats:** 59 commits, 74 files changed, +10281 / −164 LOC

## Overview

Migração do app frontend-only (localStorage) para aplicação full-stack com backend Hono, autenticação Better Auth, persistência PostgreSQL e deploy Docker. Inclui fase extra de polimento UX (Fase 7) inserida após UAT.

## Phases

### Phase 1: Backend Foundation

**Goal**: Servidor Hono rodando com Prisma conectado ao PostgreSQL
**Depends on**: nenhum (primeira fase)
**Requirements**: BACK-01

Plans:
- [x] 01-01: Instalar dependências do servidor, criar .env e .gitignore
- [x] 01-02: Criar `server/index.ts`, `server/prisma.ts` e `prisma/schema.prisma`
- [x] 01-03: Rodar `migrate dev` e verificar healthcheck do servidor

### Phase 2: Autenticação Better Auth

**Goal**: Login com email/senha e Google OAuth funcionando, sessão persistente
**Depends on**: Fase 1
**Requirements**: AUTH-01, AUTH-02, AUTH-03

Plans:
- [x] 02-01: Instalar `better-auth` e criar `server/auth.ts` com email+Google
- [x] 02-02: Registrar rotas de auth no Hono (`/api/auth/*`)
- [x] 02-03: Criar `src/lib/auth-client.ts` e `src/components/AuthGuard.tsx`

### Phase 3: API de Termos (CRUD)

**Goal**: CRUD completo de termos filtrado por usuário autenticado
**Depends on**: Fase 2
**Requirements**: BACK-02, BACK-03, BACK-04, BACK-05, AUTH-04

Plans:
- [x] 03-01: Criar `server/routes/terms.ts` com GET list, GET by id, POST upsert, DELETE
- [x] 03-02: Registrar rotas no `server/index.ts` e testar com autenticação

### Phase 4: Proxy Gemini

**Goal**: Chamadas Gemini roteadas pelo backend, chave não exposta no cliente
**Depends on**: Fase 2
**Requirements**: GEM-01, GEM-02, GEM-03

Plans:
- [x] 04-01: Criar `server/routes/gemini.ts` movendo a lógica de `services/gemini.ts`
- [x] 04-02: Registrar rotas e remover `services/gemini.ts` do frontend

### Phase 5: Migração do Frontend

**Goal**: Frontend usa API REST em vez de localStorage, com AuthGuard ativo
**Depends on**: Fases 3 e 4

Plans:
- [x] 05-01: Substituir chamadas `localStorage` em `App.tsx` por `fetch('/api/terms/...')`
- [x] 05-02: Substituir chamadas `services/gemini.ts` por `fetch('/api/gemini/...')`
- [x] 05-03: Integrar `AuthGuard` no `App.tsx` e testar fluxo completo

### Phase 6: Docker + Deploy

**Goal**: App containerizada e acessível na porta 8095
**Depends on**: Fase 5
**Requirements**: DEPL-01, DEPL-02

Plans:
- [x] 06-01: Criar `Dockerfile.api`, `Dockerfile.nginx` e `nginx.conf`
- [x] 06-02: Criar `docker-compose.yml` com serviços postgres, api, nginx
- [x] 06-03: Build + smoke test completo no Docker

### Phase 7: Melhorias de UX pós-UAT (INSERTED)

**Goal**: Tela de login legível com Google OAuth e editor com fluxo claro
**Depends on**: Fase 6
**Inserida após UAT** — surgiram lacunas de contraste, branding Google e UX de criação de termos.

Plans:
- [x] 07-01: Dark theme completo em `AuthGuard.tsx`
- [x] 07-02: SVG Google "G" 4-cores + separador "ou" + aria-label
- [x] 07-03: Botão "Novo Termo" na sidebar + melhorias no botão "Carregar" do Histórico

**UAT:** 3/3 passou. Bug encontrado e corrigido durante UAT: `initialData` continha sample data hardcoded; criado `emptyData` separado para o "Novo Termo" produzir formulário em branco.

---

## Milestone Summary

**Decimal Phases:** Nenhuma (Fase 7 foi adicionada após v1.0 ser definida com 6 fases, mas numerada sequencialmente).

**Key Decisions:**
- Stack: Hono + Node.js + Better Auth + Prisma + PostgreSQL — alinhado com `smarttech` e `Smart-tech-reports-v2`
- Auth: Google OAuth com mesmas credenciais dos outros apps Smart Tech
- Deploy: Docker Compose + nginx na porta 8095 (reservada no REGISTRY desklime)
- Domínio: `smart-tech-termos.institutosintonia.com.br` via Cloudflare tunnel
- `BETTER_AUTH_URL` aponta para o domínio público (não localhost), corrigido durante deploy

**Issues Resolved:**
- Wave 1 worktree merge silencioso na Fase 7 — recuperado via cherry-pick (anti-pattern documentado)
- `BETTER_AUTH_URL` originalmente apontava para `http://10.0.0.227:8095`, ajustado para o domínio Cloudflare
- "Novo Termo" restaurava sample data em vez de limpar — fix aplicado durante UAT (emptyData)

**Issues Deferred:** Nenhum identificado.

**Technical Debt Incurred:**
- `initialData` em `App.tsx` ainda contém sample data hardcoded (Prefeitura de Ipanema). Aceitável como demo seed para primeiro acesso, mas avaliar remoção em v1.1.
- Phase 7 sem `requirements:` declarado nos PLANs (campo `requirements: []`).

---

_Para status atual do projeto, ver `.planning/ROADMAP.md`._
